h1 New Artist

= simple_form_for(@artist) do |f|
  = render 'form', f: f
  .form-actions
    => f.button :submit
    = link_to Artist, class: 'btn btn-secondary' do
      | Cancel
      =< fa_icon 'close'


